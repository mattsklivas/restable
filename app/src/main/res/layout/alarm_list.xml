<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:id="@+id/parent_layout"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/wakeTimeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/alarmClockImageView"
        android:text="@string/time"
        android:textSize="22sp"
        android:layout_marginStart="10dp"
        android:paddingTop="2dp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/hoursTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/wakeTimeTextView"
        android:layout_toEndOf="@id/alarmClockImageView"
        android:text="@string/h_h"
        android:layout_alignStart="@id/wakeTimeTextView"
        android:textSize="18sp"
        android:paddingBottom="2dp"/>

    <TextView
        android:id="@+id/ratingTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:text="@string/rating"
        android:layout_alignParentEnd="true"
        android:layout_alignBaseline="@id/hoursTextView"
        android:layout_marginEnd="20dp"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/hoursOfSleepTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:text="@string/hours_of_sleep"
        android:layout_toEndOf="@id/hoursTextView"
        android:layout_marginStart="5dp"
        android:layout_alignBaseline="@id/hoursTextView"
        android:textSize="18sp" />

    <ImageView
        android:id="@+id/alarmClockImageView"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="10dp"
        android:contentDescription="@string/clock_logo"
        app:srcCompat="@drawable/ic_baseline_add_alarm_24" />

    <TextView
        android:id="@+id/setTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/wakeTimeTextView"
        android:layout_alignEnd="@id/ratingTextView"
        android:text="@string/alarm_set"
        android:textColor="@color/colorAccent"
        android:textSize="18sp"
        android:textStyle="bold"
        android:visibility="gone" />


</RelativeLayout>